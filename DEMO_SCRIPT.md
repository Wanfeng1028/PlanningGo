# PlanningGo 演示脚本

## 环境准备

### 1. 启动后端

```bash
# 设置环境变量（.env 或 shell）
export DEMO_MODE=true
export PLANNING_MODE=mock
export AGENT_CHAT_MODE=auto

# 启动
npm run dev:server
```

### 2. 启动前端

```bash
npm run dev:client
```

### 3. 打开浏览器

访问 `http://localhost:5173`，进入 FeaturesPage（规划工作台）。

---

## 演示场景 1：家庭周末半日游

### 触发方式

**方式 A — 点击演示场景按钮**

在首页点击「👨‍👩‍👧‍👦 家庭周末半日游」卡片。

**方式 B — 手动输入**

```
这周六带老婆和6岁儿子在杭州周边玩半天，不想太累，想找个亲子友好的地方，午餐就近。
```

### 预期结果

1. **工具链路面板**显示：
   - ✓ 解析意图：人群=家庭(亲子)，时间=周六，城市=杭州
   - ✓ 搜索 POI：亲子活动、餐厅
   - ✓ 生成候选方案
   - ✓ 评分排序
   - ✓ 生成时间线

2. **方案卡片**显示：
   - 标题类似「亲子半日游：西溪湿地 + 周边午餐」
   - 人群标签：适合家庭/亲子
   - 约束标签：👨‍👩‍👧 亲子友好、⏱ 半天、💰 预算范围
   - 时间线包含：景点 + 餐厅

3. 点击「选这套方案」后，显示下一步操作按钮

---

## 演示场景 2：情侣约会

### 触发方式

**方式 A — 点击演示场景按钮**

在首页点击「💑 情侣约会」卡片。

**方式 B — 手动输入**

```
这周日和女朋友在杭州约会一天，想要浪漫一点，可以看电影、喝咖啡，晚餐环境好。
```

### 预期结果

1. **工具链路面板**显示：
   - ✓ 解析意图：人群=情侣，时间=周日，城市=杭州
   - ✓ 搜索 POI：影院、咖啡厅、餐厅
   - ✓ 生成候选方案
   - ✓ 评分排序（偏好高分、环境好）
   - ✓ 生成时间线

2. **方案卡片**显示：
   - 标题类似「浪漫约会日：银泰观影 + 咖啡 + 晚餐」
   - 约束标签：💑 情侣、🎬 电影、☕ 咖啡、🍽 环境好
   - 时间线包含：电影 + 咖啡 + 晚餐

---

## 演示场景 3：朋友聚会

### 触发方式

**方式 A — 点击演示场景按钮**

在首页点击「🎉 朋友聚会」卡片。

**方式 B — 手动输入**

```
这周六和三个朋友在杭州聚会，想要有趣的活动，可以一起玩的，晚上一起吃火锅。
```

### 预期结果

1. **工具链路面板**显示：
   - ✓ 解析意图：人群=朋友(4人)，时间=周六，城市=杭州
   - ✓ 搜索 POI：娱乐活动、火锅餐厅
   - ✓ 生成候选方案
   - ✓ 评分排序
   - ✓ 生成时间线

2. **方案卡片**显示：
   - 标题类似「朋友聚会日：桌游 + 海底捞」
   - 约束标签：🎉 4人聚会、🎮 娱乐、🍲 火锅
   - 时间线包含：桌游/娱乐 + 火锅

---

## 演示场景 4：预约失败 + 降级（可选）

### 触发方式

```bash
# 启动后端时设置
export MOCK_BOOKING_FAILURES=no_seat,no_ticket
```

### 预期行为

1. 当方案中包含餐厅或景点时，`bookingAvailable` 会被设为 `false`
2. 方案卡片上会显示风险标签：⚠ "当前时段已无可用座位"
3. Agent 在生成方案时会推荐备选方案（如其他餐厅或改期）
4. 工具链路面板显示降级决策过程

---

## 演示场景 5：延迟模拟（可选）

### 触发方式

```bash
# 启动后端时设置
export MOCK_LATENCY_MS=2000
```

### 预期行为

1. 所有 POI 搜索和 booking 操作会延迟 2 秒
2. 前端工具链路面板会显示 "运行中" 状态
3. 验证延迟时的用户体验（loading 状态正确）

---

## 关键 UI 元素检查清单

| 元素 | 位置 | 说明 |
|------|------|------|
| 演示场景按钮 | 首页标题下方 | 3 张卡片，hover 有金色边框效果 |
| 工具链路面板 | 聊天界面右上角 | 折叠/展开，显示执行步骤 |
| 约束标签 | 方案卡片底部 | 紫色背景，人群/预算/步行等 |
| 风险标签 | 方案卡片底部 | 红色背景，预约失败时显示 |
| 备选方案提示 | 方案卡片底部 | 灰色背景，降级时显示 |

---

## 常见问题

**Q: 演示场景按钮点击后没反应？**

A: 确保后端已启动（`npm run dev:server`），且 `PLANNING_MODE=mock`。

**Q: 工具链路面板不显示？**

A: 在聊天过程中才会显示，发送消息后等待 Agent 执行。

**Q: 方案卡片没有约束标签？**

A: 约束标签在方案生成后自动添加，确保 `PLANNING_MODE=mock` 或 LLM 正常工作。
